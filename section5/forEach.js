/*
    FOR EACH
    Um método que incide sobre arrays, recebe uma função que, automaticamente recebe três parâmetros.
*/

const numeros = [5, 50, 80, 1,];

numeros.forEach((valor, indice, array) =>{
    console.log(valor, indice, array)
})